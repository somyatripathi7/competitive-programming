#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    int t;
    cin>>t;
    while(t--) {
        int n; cin>>n;
        vector<int> o;
        vector<int> e;
        for(int i=1;i<=n;i++) {
            int a; cin>>a;
            o.push_back(a);
        }
        for(int i=1;i<=n;i++) {
            int a; cin>>a;
            e.push_back(a);
        }

    }
}